spring:
  datasource:
    url: jdbc:h2:mem:filepolicy_test;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
  flyway:
    enabled: true
  thymeleaf:
    cache: false
